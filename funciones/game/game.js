const { Variables } = require("../../models/variables");
const Soket = require("../../socket/socket");


const Game = (props) => {
  const { http = false } = props;
  if (http) {
    
  }
};

module.exports = Game;